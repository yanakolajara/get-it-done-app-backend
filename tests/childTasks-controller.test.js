const {
  getChildTasksFromTaskId,
  getAllChildTasks,
  getTaskOnTopOfQueue,
  createChildTask,
  editChildTaskPosition,
} = require("../models/childTasks-model");

test("Draft test", () => {
  expect("draft test").toBe("draft test");
});
